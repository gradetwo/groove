{
  "version": "2.1.8",
  "releaseDate": "2026-09-20",
  "changelogCount": 172,
  "latest": {
    "version": "2.1.8",
    "date": "2026-09-20",
    "category": "fix",
    "title": {
      "zh": "v2.1.8 走带条钉在页眉之下——顺带发现 `position: sticky` 在这个应用里从来没生效过",
      "en": "v2.1.8 The Transport Is Pinned Under the Header — and `position: sticky` Never Worked Anywhere in This App"
    },
    "highlights": [
      {
        "zh": "**走带条现在常驻页眉之下（约 52px）**：播放 / BPM / 小节 / 图案槽 / 撤销重做被提取成一条独立的 sticky 条，编辑 / 视图 / 工程组照旧随页面滚——常驻的是一行，不是整条工具栏。之前滚到底时传输组在 `top −247`、中心命中为空；现在停在页眉下方，命中测试打在它自己身上。",
        "en": "**The transport now parks under the header (~52 px).** Play / BPM / bar / pattern slots / undo-redo moved into their own sticky strip, while the edit, view and project groups still scroll with the page — one row is permanent, not a whole toolbar. Before, scrolled to the bottom, the group sat at `top −247` and hit-tested as nothing; now it stops right below the header and the hit test lands on it."
      },
      {
        "zh": "**实现时才查出真因：这个应用里没有任何 `position: sticky` 生效过。** `<body>` 上挂着 `overflow-x-hidden`，而 `overflow: hidden` 会让元素成为滚动容器——body 因此成了所有 sticky 后代的最近 scrollport，偏偏 body 是随页面一起滚的，于是连声明了 `sticky top-0 z-50` 的应用页眉也在滚 400px 后停在 `top −400`。改用 `overflow-x: clip`（裁剪同样像素，但不创建滚动容器）后 sticky 恢复工作，**页眉第一次真的会吸顶**。`hidden` 作为老 Safari 的回退保留在前。",
        "en": "**Implementing it uncovered the real cause: `position: sticky` had never worked anywhere in this app.** `<body>` carried `overflow-x-hidden`, and `overflow: hidden` makes an element a scroll container — so `body` became the nearest scrollport for every sticky descendant, and since `body` scrolls *with* the page rather than inside it, even the app header (declared `sticky top-0 z-50`) sat at `top −400` after a 400 px wheel. Switching to `overflow-x: clip` — same clipping, no…"
      },
      {
        "zh": "**门禁是失败可证的**：E2E 在 5 个桌面/iPad 目标上断言 `--app-header-h` 等于页眉实测高度、滚到底后走带条在视口内**且中心命中它自己**（在视口里但被页眉盖住是点不到的，纯几何测不出来）、并停靠在页眉之下 0–8px。把 `sticky` 从条上拿掉重建，它报 `top −462, at its centre nothing`。`stickyAnchors.test.ts`（7 条）把 `<body>` 不得再用 `overflow-x-hidden`、样式表必须用 `clip`、两个 sticky 元素锚定同一 token、走带条必须是工具栏的**同级兄弟**（sticky 只能在自己父盒内移动，工具栏只有 116px 高，这是第一版失败的原因）钉在源码上。",
        "en": "**The new gates can fail.** The E2E asserts, on five desktop/iPad targets, that `--app-header-h` equals the header's rendered height, that the transport is inside the viewport **and that the point in its middle resolves to it** (an element in the viewport but behind the header cannot be clicked, and geometry alone would pass it), and that it parks 0–8 px under the header. Removing `sticky` from the strip and rebuilding makes it fail with `top −462, at its centre nothing`. Se…"
      }
    ],
    "truncated": true
  }
}
