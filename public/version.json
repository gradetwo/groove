{
  "version": "2.19.0",
  "releaseDate": "2026-09-24",
  "changelogCount": 213,
  "latest": {
    "version": "2.19.0",
    "date": "2026-09-24",
    "category": "feature",
    "title": {
      "zh": "v2.19.0 循环素材能自己接上了：渲染尾巴折回开头；导出多了一个无缝循环选项",
      "en": "v2.19.0 Loop assets join themselves: the render's tail folds back over the head, behind a `seamlessLoop` export option"
    },
    "highlights": [
      {
        "zh": "**循环不再是一遍放完、余音拖出去、再从头开始。** 渲染尾巴本身是对的（文件不能在混响还在响的时候停），但把它接在循环后面就错了：接缝既晚又轻。现在把尾巴按循环长度折回开头——也就是「假如循环从没停过，混响本来会在做的事」——导出的 `seamlessLoop` 选项给出的文件正好是一个循环的长度，重复播放时听到的是那一次衰减，而不是一段空白加一次重启。两条返回路径（worklet 限幅与回退限幅）走同一个出口，所以这个保证不取决于平台是否支持 AudioWorklet。",
        "en": "**A loop no longer plays, rings out and restarts.** The render tail is right for a file (it must not stop while the reverb is audible) and wrong for a loop: the seam is both late and quiet. The tail is now folded back over the head modulo the loop length — what the reverb would have been doing had the loop never stopped — so the `seamlessLoop` export option returns exactly one loop, and repeating it reproduces that decay instead of a gap followed by a restart. Both return pa…"
      },
      {
        "zh": "**顺带两条测量上的诚实**：① 曲风质量门禁的 `narrowStereo` 预算按规则从 9 收到 **7**——两次独立分片都读到 7（相邻风格那三个 0.98 上下的曲风已经挪开），而 `flatStabs` / `cutTail` 因为两次读数不同（2/1、0/1）继续保留最差值；② 会「听」应用的实时编排探针以前取首尾三分之一的**均值**，在共享 runner 上把一次卡顿读成「渐强没抬起来」（同一棵树在本地是 +10 %、CI 上是 3.4 %）。现在它按**每一遍的中位数**比较开头那遍与最响的一遍，并且把「平台掉帧」单独报成 `unmeasured`——对机器的判断和对混音的判断不再混为一谈。",
        "en": "**Two measurement fixes alongside it.** (1) The quality gate's `narrowStereo` budget ratchets 9 → **7**, because the rule is that a budget is the worst reading *and* the tightening waits for a render that repeats: two independent shard dispatches both read 7, while `flatStabs` and `cutTail` keep their worst reading (2/1 and 0/1) for exactly the same reason. (2) The live arrangement probe — the only one that listens to the running app — used to average the first and last thir…"
      }
    ],
    "truncated": false
  }
}
