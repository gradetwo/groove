{
  "version": "2.0.94",
  "releaseDate": "2026-09-19",
  "changelogCount": 158,
  "latest": {
    "version": "2.0.94",
    "date": "2026-09-19",
    "category": "audio",
    "title": {
      "zh": "v2.0.94 带独立 swing 的轨道不再比别的轨道早一个延迟补偿量",
      "en": "v2.0.94 A Track With Its Own Swing No Longer Fires a Latency Offset Early"
    },
    "highlights": [
      {
        "zh": "**逐轨 swing 那条分支自己重建了时间，于是丢掉了两样东西。** 计划里只写了丢掉 `latencyCompensationMs`，实际连「不得早于 `currentTime`」的钳位一起丢了。结果是：带独立 swing 的轨道比其它轨道**整整早一个补偿量**，也比它自己的导出早（导出是离线渲染，没有输出延迟可补，本来就不该有这一项）。",
        "en": "**The per-track swing branch rebuilt the scheduled time from scratch and dropped two things.** The plan named only the lost `latencyCompensationMs`; the `>= currentTime` clamp went with it. A track with independent swing therefore fired a full compensation ahead of every other track — and ahead of its own export, which is an offline render with no output latency to compensate in the first place."
      },
      {
        "zh": "**修法只有一句：这个分支里只有 swing 项允许不同。** 延迟补偿与钳位照抄调用方的语义，另加一个局部 `ctx` 收窄（`this.ctx` 在回调里会重新变宽成 nullable）。",
        "en": "**The fix is one expression: only the swing term may differ here.** The latency offset and the clamp now mirror what the caller already folded into `time`, plus a local `ctx` narrowing because `this.ctx` widens back to nullable inside the callback."
      },
      {
        "zh": "**动手前先逐行核对了 M 表，14 行里 4 行写的缺陷早就不存在了**：M2（母带 DRIVE 硬削波）、M6（推子阶跃）、M7（延迟反馈阶跃）、M10（实时 ratchet 未夹取）都已修过，行号也全都过期。已在计划里逐条标注。顺手把实时路径里**复制的那份**副拍力度渐变换成共享的 `ratchetVelocityScale`——公式一样，改的是「同一件事只留一份」。",
        "en": "**The M table was audited line by line before touching anything: four of its fourteen rows described defects that no longer exist** — M2 (master DRIVE hard clip), M6 (fader stepping), M7 (delay feedback stepping) and M10 (unclamped live ratchet) were all already fixed, and every line reference had drifted. Each row now says so. The live path also stopped carrying its own copy of the sub-hit velocity taper and calls the shared `ratchetVelocityScale` instead: same formula, one…"
      }
    ],
    "truncated": true
  }
}
