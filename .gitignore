# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Testing
coverage

# Scratch measurement reports (a subset run must never be committed as a baseline)
scripts/*.partial.json

# Python
__pycache__/
*.pyc

# Deploy packages
*.tar.gz
*.zip
deploy-package/
release/
scratch/

# Local secrets / deploy credentials
.env
.env.*
!.env.example
!.env.deploy.example

# Local tooling state
.wrangler/
.worktrees/
.vite/

# Slow-pack handoff output (scripts/slow_pack.mjs) — packages travel by hand, not by git.
slowpack-out/
slowpack-*.tar.gz
slowpack-*.tar.gz.sha256

# Cross-device E2E matrix artifacts (scripts/test_matrix.js)
e2e-out/

# Transient measurement progress files (regenerated by every measure run)
scripts/*.progress.json
wrangler.toml
