{
  "version": "2.0.95",
  "releaseDate": "2026-09-19",
  "changelogCount": 159,
  "latest": {
    "version": "2.0.95",
    "date": "2026-09-19",
    "category": "audio",
    "title": {
      "zh": "v2.0.95 限幅器的前瞻窗口不再每样本全扫一遍",
      "en": "v2.0.95 The Limiter's Lookahead Window No Longer Scans Every Sample"
    },
    "highlights": [
      {
        "zh": "**限幅器的滑窗最小值原来是每样本扫 145 次（D=144 @3 ms/48 kHz），而且它在渲染线程里。** 现在是一个**单调队列**：队首就是窗口最小值，O(1) 读出，每个样本入队一次、出队至多一次。**输出逐位不变**——入队时值先过 `Math.fround`，正是旧代码写进 `Float32Array` 时发生的转换，所以「选中的最小值」和旧扫描完全一致。",
        "en": "**The limiter's sliding-window minimum used to compare 145 values per sample (D = 144 at 3 ms/48 kHz), on the render thread.** It is now a **monotonic deque**: the smallest value in the window sits at the front and is read in O(1), each sample is pushed once and popped at most once. **The output is bit-identical** — values enter through `Math.fround`, exactly the conversion the old `Float32Array` window applied on write, so the minimum that gets chosen is the same one the sc…"
      },
      {
        "zh": "**实测工作量**（把窗口这一步单独拿出来跑，因为改动只在它）：4,410,000 样本、窗口 145，比较次数 **639,450,000 → 8,549,300**（145.0 → **1.939/样本**），该步耗时 **2126 ms → 127 ms**，校验和一致。这是窗口步的数字，不是整条限幅链的加速比——4×12 抽头的真峰卷积没动。",
        "en": "**Measured work** (the window step alone, since that is all that changed): over 4,410,000 samples with a 145-sample window, comparisons fall from **639,450,000 to 8,549,300** (145.0 → **1.939 per sample**) and the step's time from **2126 ms to 127 ms**, with identical checksums. That is the window step, not the whole limiter — the 4×12-tap true-peak convolution is untouched."
      },
      {
        "zh": "**两份实现都改了，因为真正的实时限幅器不是 TypeScript 那份**：`public/limiterWorklet.js` 是逐行镜像（`src/**` 进不了音频线程），只改内核等于没优化生产路径。两道防线：原有的 worklet 对拍测试（逐字执行 shipped 源码、断言两份 sample-identical）只改一份就会红；另加一条**源码守卫**要求两边都用队列、都不再有每样本扫描。源码级断言是故意的——输出已经无法区分两种实现，「扫描被重新引入」在行为上永远测不出来。",
        "en": "**Both copies changed, because the realtime limiter is not the TypeScript one**: `public/limiterWorklet.js` is a line-for-line mirror (`src/**` cannot reach the audio thread), so fixing only the kernel would leave production unoptimised. Two guards: the existing worklet parity test (it evaluates the shipped source verbatim and asserts the two are sample-identical) fails if only one side changes, and a new **source guard** requires the deque in both files and no per-sample sc…"
      }
    ],
    "truncated": true
  }
}
