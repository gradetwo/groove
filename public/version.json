{
  "version": "2.0.82",
  "releaseDate": "2026-09-19",
  "changelogCount": 146,
  "latest": {
    "version": "2.0.82",
    "date": "2026-09-19",
    "category": "audio",
    "title": {
      "zh": "v2.0.82 立体声合唱真的立体声了：两个抽头各走一个声道，并反向调制",
      "en": "v2.0.82 The Stereo Chorus Is Stereo: Each Tap Gets Its Own Channel, Modulated in Anti-Phase"
    },
    "highlights": [
      {
        "zh": "**湿声是两个抽头共用的「单声道和」。** `ChannelMergerNode` 的每个输入按规范只有 1 个声道，而两个抽头是从粉碎级直接接进合并器的——于是立体声总线在这里被**降混**。一个本来用来加宽声场的效果把声像塌成了单声道，左右唯一的差别只剩固定的 15 / 22 ms 延迟。现在湿分支先经过一个显式两声道增益再分流：`crusherNode → chorusStereo(2ch) → ChannelSplitter(2) → 左右各一个延迟 → merger(2)`，左抽头 = 声道 0、右抽头 = 声道 1。那个显式上混是必须的：分流器遇到单声道输入时输出 1 是静音，全居中的混音会把右抽头整个丢掉。",
        "en": "**The wet signal was one mono sum shared by both taps.** A `ChannelMergerNode` input is one channel wide by specification, and both taps were fed straight into it from the crusher — so the stereo bus was **down-mixed** there. An effect that exists to widen the image was collapsing it, leaving the fixed 15/22 ms delay as the only difference between the sides. The wet branch now forces two channels and splits: `crusherNode → chorusStereo(2ch) → ChannelSplitter(2) → per-side de…"
      },
      {
        "zh": "**两侧由同一个 LFO 同相驱动。** 一个深度增益同时接两个延迟时间，两侧一起摆动——那是「双单声道」合唱。现在改成两个增益、一正一负（`+3 ms` / `−3 ms`），两侧反向摆动，正是 Juno 那一代立体声合唱的做法。两个基延迟是 15 / 22 ms，±3 ms 摆动不会把延迟时间推到负值；调制深度也提成了一个常量 `CHORUS_DEPTH_SEC`。",
        "en": "**One LFO drove both sides in phase.** A single depth gain fed both delay times, so the sides moved together — a dual-mono chorus. There are now two gains, `+3 ms` and `−3 ms`, so the sides move against each other, which is the Juno-era arrangement. Both base delays (15/22 ms) are far enough from zero that 3 ms of swing cannot drive a delay time negative, and the depth is now a single named constant, `CHORUS_DEPTH_SEC`."
      },
      {
        "zh": "**测试（`chorusStereo.test.ts`，6 条）**：分流器两个输出分别喂左右抽头、抽头不再取自分流前的节点、分流前那个增益是 `channelCount 2 / explicit / speakers`、两侧回到合并器的不同输入、两个深度幅度相同符号相反、关掉合唱后两个深度都被释放，以及**关闭时湿增益为 0 且不创建 LFO**。这一批断言能量出来，是因为顺手给测试用的 `FakeNode` 加了 `outgoing`（记录每条边用的输出/输入**序号**）——`incoming` 分不出分流器的两个输出，而「哪个输出喂哪个抽头」正是这次的缺陷。把旧接线放回去实测 **2 条失败**；全量单测 176 文件 / 2039 用例。",
        "en": "**Tests (`chorusStereo.test.ts`, 6 cases):** the splitter's two outputs feed the two taps, the taps no longer come from the pre-split node, that node is `channelCount 2 / explicit / speakers`, the taps return on separate merger inputs, the two depths are equal and opposite, both are released when the chorus is switched off, and a bypassed chorus has a zero wet gain and no LFO at all. These are assertable because the test `FakeNode` gained `outgoing` (every edge with its outp…"
      }
    ],
    "truncated": true
  }
}
